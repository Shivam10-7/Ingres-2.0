1. make a query exectutot that executes  sql query.
2. make a query validator that checks that not commnads like drop,delete and alter like commands are executed on the database.
3. if database ruturns many rows try to normalize and reduse to 10 or minimum.
4. If all this is done then you will work with kushal and me for backend-integration.